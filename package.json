{
  "name": "mittwald-ai-playground",
  "private": true,
  "workspaces": ["client", "server"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -k \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "overrides": {
    "shell-quote": "^1.8.4"
  }
}
