node_modules
dist
.env
.env.local
.env.production
*.log
PRIVAT-*.md
.DS_Store
client/dist

# Lokale Test-PDFs (z. B. Rechnungs-OCR im Workspace-Root)
/*.pdf

# CRM-Rohdaten (personenbezogen — nicht committen, nicht deployen)
docs/customer.js

# Lokale Railpack-Artefakte (mw experimental deploy)
railpack-info.json
railpack-plan.json

# Lokales Deploy-Skript (projektspezifisch, nicht ins Repo)
scripts/deploy-mittwald.sh
.mittwald-api-token
